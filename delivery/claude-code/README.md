# delivery/claude-code — empaque para Claude Code (fase 4)

Genera skills con frontmatter estándar (`SKILL.md`), `AGENTS.md`/`CLAUDE.md` y un
instalador `.sh`/`.mjs`. Mismo contenido que `core/`, distinto contenedor.
