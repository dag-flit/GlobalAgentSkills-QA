# adapter azure-devops (stub F0 → completo en F2)

En F0 solo declara `capabilities()` y un `preflight()` que verifica presencia de
variables `.env`. La lógica MCP/REST (política dual de evidencias, campos de ciclo,
cierre de Task/Bug) se porta desde el kit v3 en la fase 2.
