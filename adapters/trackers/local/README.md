# adapter local (default)

Sin red, sin PAT. Preflight siempre OK. Lee requisitos de `.qa/work-items/{id}.md`
(o devuelve stub) y escribe evidencia md+html en `qa-evidence/`. Es el destino por
defecto cuando `tracker: local`.
